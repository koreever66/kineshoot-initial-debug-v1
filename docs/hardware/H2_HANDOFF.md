# H2 Hardware Handoff

基线：

```text
baseline_id       = KB-2026-09-15-H1-F4-I1
interface_revision = I1
hardware_revision  = H2
hardware_branch    = codex/hardware-bringup
```

当前状态：H2 采购和装配方案已建立，实物尚未完成最终装配和测试。本文档中的“通过”必须在真实测量和照片完成后才能勾选。

## 1. 硬件边界

本仓库只负责：

- 硬件物料和 BOM。
- 接线和连接器。
- 底板、腕部和前臂固定。
- 电源开关和电源路径。
- 线束应力释放。
- 万用表检查和硬件测试。

本仓库不负责：

- 固件。
- 软件协议。
- CSV 和 meta 数据格式。
- 采样率、量程、触发方式。
- 数据分析和模型。

## 2. 固定接口

```text
ICM VCC  -> ESP32 3V3
ICM GND  -> ESP32 GND
ICM SDA  -> ESP32 GPIO8
ICM SCL  -> ESP32 GPIO9
ICM NCS  -> ESP32 3V3
ICM AD0  -> ESP32 GND

I2C address = 0x68
I2C clock   = 50kHz
WHO_AM_I    = 0xEA
```

禁止：

- `0x69` 或 `0x0C` 作为长期主地址。
- `NCS` 或 `AD0` 悬空。
- 切换或旁路 3.3V 电源路径。
- 改为软件未批准的连接器线序。

## 3. 连接器和线序

连接器：PH2.0 2.0mm 4P 公母带线。

```text
Pin 1 = VCC = 3V3
Pin 2 = GND = 公共地
Pin 3 = SDA = GPIO8
Pin 4 = SCL = GPIO9
```

ICM 端就近固定：

```text
ICM NCS -> 3V3
ICM AD0 -> GND
```

NCS 和 AD0 固定后不需要 6P 连接器。4P 线长优先 10 到 15cm，最多先不超过 20cm。焊接后用万用表逐根检查，不能只靠线色。

## 4. 接线图

```text
                         +----------------+
ESP32-S3 3V3 -----------| PH2.0 4P Pin 1 |-------- ICM VCC
ESP32-S3 GND -----------| PH2.0 4P Pin 2 |-------- ICM GND
ESP32-S3 GPIO8 ---------| PH2.0 4P Pin 3 |-------- ICM SDA
ESP32-S3 GPIO9 ---------| PH2.0 4P Pin 4 |-------- ICM SCL

ESP32-S3 3V3 ------------------------------ ICM NCS
ESP32-S3 GND ------------------------------ ICM AD0

ICM-20948 和 ESP32-S3 必须共地
```

## 5. 机械固定

### 腕部传感器单元

- ICM 放入 `5cm × 3.5cm × 1.8cm` 小盒。
- 盒底贴 2mm EVA。
- 盒子背面贴魔术贴，固定到 3.8cm 腕带。
- 位置：腕横纹向上约 2 到 4cm。
- 盒子画箭头，箭头始终朝向手肘。

### 前臂电源和主控单元

- 第一版把 ESP32、TP4057、电池放入 `10cm × 8cm` 布艺软袋。
- 前臂使用 5cm 绑带，位置约在腕横纹向上 6 到 10cm，避开肘窝。
- `10cm × 7cm × 2.3cm` 主控盒只用于运输和台架测试，不作为投篮佩戴外壳。
- 袋内电池和主板之间加 EVA 或薄泡棉，防止互相撞击。

## 6. 电源路径和开关

```text
500mAh 电池 B+ -> PH2.0 2P -> TP4057 BAT+
500mAh 电池 B- -> PH2.0 2P -> TP4057 BAT-

TP4057 另一组 BAT+ -> 开关 -> 3.7V转5V升压 IN+
TP4057 另一组 BAT- --------> 3.7V转5V升压 IN-

3.7V转5V升压 OUT+ -> ESP32 5V
3.7V转5V升压 OUT- -> ESP32 GND
```

规则：

- 开关放在 5V 路径，不切换 3.3V。
- TP4057 输出不是 5V，必须经过升压模块。
- USB 烧录或导出时断开电池输出。
- 充电时关闭负载，第一版不要把封闭软袋直接放在无人看管的充电环境。

## 7. 应力释放

- 线束两端各留 1 到 2cm 缓冲弯。
- 每 3 到 5cm 用纸胶带、魔术贴或医用胶带固定。
- PH2.0 插头前后不能直接承受传感器惯性拉力。
- 传感器盒入口处先固定线束，再进入盒子。
- 前臂软袋入口处固定线束，USB-C 口不受线缆拉力。
- 快速挥臂时，插头、焊点和 ICM 排针不能出现可见位移。

## 8. 万用表检查

### 断电检查

- [ ] VCC 对 GND 不短路。
- [ ] SDA 对 GND 不短路。
- [ ] SCL 对 GND 不短路。
- [ ] VCC 与 SDA 不导通。
- [ ] VCC 与 SCL 不导通。
- [ ] NCS 与 3V3 导通。
- [ ] AD0 与 GND 导通。
- [ ] PH2.0 4P 两端线序一致。
- [ ] 电池正负极没有接反。

### 上电检查

- [ ] ICM VCC 约 3.3V。
- [ ] NCS 约 3.3V。
- [ ] AD0 约 0V。
- [ ] ESP32 与 ICM 共地。
- [ ] 升压模块输出约 5.0V。
- [ ] ESP32 3V3 测试点约 3.3V。

## 9. H2 测试步骤

### 测试 A：接口静态检查

```text
1. 只使用 USB 给 ESP32 供电。
2. 不接电池和升压模块。
3. 软件读取 WHO_AM_I。
4. 预期返回 0xEA。
5. 固定 I2C 50kHz 和地址 0x68。
```

通过条件：

- [ ] 地址 `0x68`。
- [ ] `WHO_AM_I=0xEA`。
- [ ] 无通信异常。

### 测试 B：10 秒静止测试

```text
1. 固定传感器和线束。
2. 保持静止。
3. 使用 F4 固件短按 BOOT 记录 10 秒。
4. 导出并检查数据。
```

通过条件：

- [ ] 采样率约 220-225Hz。
- [ ] 最大采样间隔不超过 10ms。
- [ ] 无复位。
- [ ] 无全零。
- [ ] 无固定饱和。
- [ ] 无削顶。

### 测试 C：小幅动态测试

```text
1. 缓慢屈伸腕部、旋转前臂和摆臂。
2. 检查连接器和线束是否松动。
3. 再次执行 10 秒记录。
```

通过条件：

- [ ] 无复位。
- [ ] 无全零。
- [ ] 无固定饱和。
- [ ] 无削顶。
- [ ] 最大采样间隔不超过 10ms。
- [ ] 接头和焊点没有位移。

### 测试 D：真实投篮门槛

只有在测试 A、B、C 全部通过后，才允许进入真实投篮采集。

## 10. 照片要求

实物照片放入 `photos/H2/`，不得包含姓名、手机号、地址或订单页面。

必须提交：

- [ ] ESP32 和 ICM 接线全景。
- [ ] PH2.0 4P 两端插头和线序。
- [ ] ICM 的 NCS 和 AD0 固定位置。
- [ ] 电池和 TP4057 焊接位置。
- [ ] 开关和升压模块位置。
- [ ] 腕部传感器单元正面。
- [ ] 腕部传感器单元背面和 EVA。
- [ ] 前臂电池和主控软袋。
- [ ] 实际佩戴正面照片。
- [ ] 线束应力释放特写。
- [ ] 万用表电压或通断检查照片。

照片提交后，在本文件的对应复选框打勾。

## 11. 已知风险

- 3.7V 转 5V 升压模块尚未购买，当前无法完成电池供电验证。
- H2 实物尚未装配，所有机械和测试结果均为待验证。
- `10cm × 8cm` 软袋对前臂偏大，真实投篮时可能晃动。
- `10cm × 7cm × 2.3cm` 主控盒不适合佩戴，只能做收纳和台架。
- 26AWG 线只有黑红两色，SDA 和 SCL 必须靠标签区分。
- TP4057 是充电板，需要确认 Type-C 口在前臂软袋中的可访问性。
- 快速动作时，PH2.0 插头可能承受拉力，必须实际验证应力释放。
- NCS 和 AD0 如果焊点断开，可能出现地址漂移或通信异常。
- 任何接口变更都必须由软件对话批准并更新基线文件。

## 12. H2 完成判定

- [ ] BOM 已确认并记录采购状态。
- [ ] 接线图和连接器型号已确认。
- [ ] 固定方式和应力释放已实装。
- [ ] 电源路径和开关位置已实测。
- [ ] 照片已提交。
- [ ] 万用表检查已通过。
- [ ] 10 秒静止测试已通过。
- [ ] 小幅动态测试已通过。
- [ ] 已知风险已记录。

在上述项目全部完成前，H2 状态保持为“装配与验证中”，不得标记为完成。
